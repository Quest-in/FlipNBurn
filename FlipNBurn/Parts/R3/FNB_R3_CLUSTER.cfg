PART
{
	name = FNB_R3_CLUSTER
	module = Part
	author = `Questin & Recoleto

		MODEL 
	{
		model = FlipNBurn/Assets/R3/FNB_R3_CLUSTER
		position = 0, 0, 0
		rotation = -90, 0, -90
		scale = 1, 1, 1
	}
	rescaleFactor = .625


	TechRequired = advConstruction
	entryCost = 3000
	cost = 15000
	category = FuelTank
	subcategory = 0
	title = R3-Cluster
	manufacturer = SpaceX
	description = Next Generation Raptor(s)
	tags = spacex, bl3, booster, sh, superheavy, 3, block, cluster, r3, raptor, rb, rc, center, core, inner, outer
	CoMOffset = 0, 0, 0
	
	// attachment rules: stack, srfAttach, allowStack, allowSrfAttach, allowCollision
	attachRules = 1,0,1,1,0

	mass = 30.3
	dragModelType = default
	maximum_drag = 1.1
	minimum_drag = 0.9
	angularDrag = 2
	crashTolerance = 22
	breakingForce = 250
	breakingTorque = 250
	maxTemp = 12500
	dragMultiplier = 1.01
	NODE
    {
        name = node_stack_top
        transform = node_stack_top
        size = 4
        method = FIXED_JOINT
    }

	NODE
    {
        name = node_stack_bottom
        transform = node_stack_bottom
        size = 4
        method = FIXED_JOINT
    }

    MODULE
	{
		name = ModuleEnginesFX
		engineID = Raptor_3_All
		thrustVectorTransformName = Outer
		exhaustDamage = True
		ignitionThreshold = 0.1
		minThrust = 0
		maxThrust = 13440
		heatProduction = 110
		fxOffset = 0, 0, 0
		EngineType = LiquidFuel
		exhaustDamageDistanceOffset = 1.5
		PROPELLANT
		{
			name = LqdMethane
			ratio = 3
			DrawGauge = True
		}
		PROPELLANT
		{
			name = Oxidizer
			ratio = 1
		}
		atmosphereCurve
		{
			key = 0 315
			key = 1 295
			key = 12 0.001
		}
	}

    MODULE
	{
		name = ModuleEnginesFX
		engineID = Raptor_3_Inner
		thrustVectorTransformName = Inner
		exhaustDamage = True
		ignitionThreshold = 0.1
		minThrust = 0
		maxThrust = 5376
		heatProduction = 110
		fxOffset = 0, 0, 0
		EngineType = LiquidFuel
		exhaustDamageDistanceOffset = 1.5
		PROPELLANT
		{
			name = LqdMethane
			ratio = 3
			DrawGauge = True
		}
		PROPELLANT
		{
			name = Oxidizer
			ratio = 1
		}
		atmosphereCurve
		{
			key = 0 315
			key = 1 295
			key = 12 0.001
		}
	}

     MODULE
	{
		name = ModuleEnginesFX
		engineID = Raptor_3_2Inner
		thrustVectorTransformName = 2Inner
		exhaustDamage = True
		ignitionThreshold = 0.1
		minThrust = 0
		maxThrust = 1344
		heatProduction = 110
		fxOffset = 0, 0, 0
		EngineType = LiquidFuel
		exhaustDamageDistanceOffset = 1.5
		PROPELLANT
		{
			name = LqdMethane
			ratio = 3
			DrawGauge = True
		}
		PROPELLANT
		{
			name = Oxidizer
			ratio = 1
		}
		atmosphereCurve
		{
			key = 0 315
			key = 1 295
			key = 12 0.001
		}
	}

	MODULE
	{
		name = ModuleEnginesFX
		engineID = Raptor_3_Core
		thrustVectorTransformName = Core
		exhaustDamage = True
		ignitionThreshold = 0.1
		minThrust = 0
		maxThrust = 2016
		heatProduction = 110
		fxOffset = 0, 0, 0
		EngineType = LiquidFuel
		exhaustDamageDistanceOffset = 1.5
		PROPELLANT
		{
			name = LqdMethane
			ratio = 3
			DrawGauge = True
		}
		PROPELLANT
		{
			name = Oxidizer
			ratio = 1
		}
		atmosphereCurve
		{
			key = 0 315
			key = 1 295
			key = 12 0.001
		}
	}

	MODULE
	{
		name = ModuleAlternator
		RESOURCE
		{
			name = ElectricCharge
			rate = 3.0
		}
	}

	MODULE
	{
		name = ModuleSurfaceFX
		thrustProviderModuleIndex = 1
		fxMax = 0.9
		maxDistance = 75
		falloff = 1.75
		thrustTransformName = Outer
	}

	MODULE
	{
		name = ModuleSurfaceFX
		thrustProviderModuleIndex = 2
		fxMax = 0.9
		maxDistance = 40
		falloff = 1.75
		thrustTransformName = Inner
    }

        MODULE
	{
		name = ModuleSurfaceFX
		thrustProviderModuleIndex = 3
		fxMax = 0.9
		maxDistance = 40
		falloff = 1.75
		thrustTransformName = 2Inner
    }

    MODULE
	{
		name = ModuleSurfaceFX
		thrustProviderModuleIndex = 4
		fxMax = 0.9
		maxDistance = 22
		falloff = 1.75
		thrustTransformName = Core
	}

	MODULE
	{
		name = ModuleGimbal
		gimbalTransformName = Core
		gimbalRange = 8
 		gimbalResponseSpeed = 8
 		useGimbalResponseSpeed = true
	}

    MODULE
	{
		name = ModuleGimbal
		gimbalTransformName = 2Inner
		gimbalRange = 8
 		gimbalResponseSpeed = 8
 		useGimbalResponseSpeed = true
	}

	MODULE
	{
		name = ModuleGimbal
		gimbalTransformName = Inner
		gimbalRange = 8
 		gimbalResponseSpeed = 8
 		useGimbalResponseSpeed = true
	}

	MODULE
	{
       name = ModuleSEPEngineSwitch
	   WaterfallControllerName = EngineMode
	   EngineID = Raptor_3_All
	   EngineID = Raptor_3_Inner
       EngineID = Raptor_3_2Inner
	   EngineID = Raptor_3_Core
	}

    // God is dead and we have killed him.... I understand Kari's pain and respect Kari's drive more than ever.

	MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer1
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform1
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer2
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform2
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer3
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform3
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer4
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform4
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer5
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform5
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer6
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform6
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer7
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform7
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer8
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform8
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer9
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform9
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer10
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform10
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer11
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform11
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer12
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform12
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer13
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform13
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer14
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform14
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer15
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform15
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer16
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform16
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer17
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform17
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer18
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform18
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer19
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform19
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3outer20
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_All
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_All
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = OuterTransform20
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3core1
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Core
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Core
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = CoreTransform1
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3core2
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Core
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Core
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = CoreTransform2
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3core3
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Core
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Core
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = CoreTransform3
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3Inner1
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = InnerTransform1
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3Inner2
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = InnerTransform2
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3Inner3
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = InnerTransform3
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3Inner4
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = InnerTransform4
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3Inner5
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = InnerTransform5
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3Inner6
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = InnerTransform6
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3Inner7
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = InnerTransform7
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3Inner8
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = InnerTransform8
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3InnerExtra1
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_2Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_2Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = Inner2Transform1
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

   MODULE
  {
    name = ModuleWaterfallFX
    // This is a custom name and should be unique in the config. 
    // It will also be used for UI display
    moduleID = raptor3InnerExtra2
    // This links the effects to a given ModuleEngines. 
    // If not found or not specified, it will use the first one it finds
    engineID = Raptor_3_2Inner
	// List out all controllers we want available
    // This controller scales with atmosphere depth
    CONTROLLER
    {
      name = atmosphereDepth
      linkedTo = atmosphere_density
    }
    // This controller scales with effective throttle
    CONTROLLER
    {
      name = throttle
      linkedTo = throttle
      engineID = Raptor_3_2Inner
    }

    TEMPLATE
    {
      // This is the name of the template to use
      templateName = waterfall-methalox-upper-raptor-1
      // This field allows you to override the parentTransform name in the EFFECTS contained in the template
      overrideParentTransform = Inner2Transform2
      // scale the templated effect
      scale = 0.7,0.7,0.7
      // rotate the templated effect
      rotation = 180,0,0
      // reposition the templated effect
      position = 0,0,0
	}
  }

  // We're almost there, still even more pain, but we're almost there.

   MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1
            rotatorsName = GimbalBall1
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1
            rotatorsName = Gimbal1
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2
            rotatorsName = Gimbal2
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2
            rotatorsName = GimbalBall2
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall
            rotatorsName = TurbopumpBall2
			matchRotation = true
        }
	}

	 MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.001
            rotatorsName = GimbalBall1.001
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.001
            rotatorsName = Gimbal1.001
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.001
            rotatorsName = Gimbal2.001
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.001
            rotatorsName = GimbalBall2.001
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.001
            rotatorsName = TurbopumpBall2.001
			matchRotation = true
        }
	}

	 MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.002
            rotatorsName = GimbalBall1.002
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.002
            rotatorsName = Gimbal1.002
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.002
            rotatorsName = Gimbal2.002
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.002
            rotatorsName = GimbalBall2.002
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.002
            rotatorsName = TurbopumpBall2.002
			matchRotation = true
        }
	}
	 MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.003
            rotatorsName = GimbalBall1.003
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.003
            rotatorsName = Gimbal1.003
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.003
            rotatorsName = Gimbal2.003
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.003
            rotatorsName = GimbalBall2.003
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.003
            rotatorsName = TurbopumpBall2.003
			matchRotation = true
        }
	}
	 MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.004
            rotatorsName = GimbalBall1.004
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.004
            rotatorsName = Gimbal1.004
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.004
            rotatorsName = Gimbal2.004
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.004
            rotatorsName = GimbalBall2.004
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.004
            rotatorsName = TurbopumpBall2.004
			matchRotation = true
        }
	}
	 MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.005
            rotatorsName = GimbalBall1.005
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.005
            rotatorsName = Gimbal1.005
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.005
            rotatorsName = Gimbal2.005
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.005
            rotatorsName = GimbalBall2.005
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.005
            rotatorsName = TurbopumpBall2.005
			matchRotation = true
        }
	}
	 MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.006
            rotatorsName = GimbalBall1.006
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.006
            rotatorsName = Gimbal1.006
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.006
            rotatorsName = Gimbal2.006
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.006
            rotatorsName = GimbalBall2.006
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.006
            rotatorsName = TurbopumpBall2.006
			matchRotation = true
        }
	}
     MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.007
            rotatorsName = GimbalBall1.007
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.007
            rotatorsName = Gimbal1.007
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.007
            rotatorsName = Gimbal2.007
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.007
            rotatorsName = GimbalBall2.007
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.007
            rotatorsName = TurbopumpBall2.007
			matchRotation = true
        }
	}
     MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.008
            rotatorsName = GimbalBall1.008
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.008
            rotatorsName = Gimbal1.008
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.008
            rotatorsName = Gimbal2.008
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.008
            rotatorsName = GimbalBall2.008
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.008
            rotatorsName = TurbopumpBall2.008
			matchRotation = true
        }
	}
     MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.009
            rotatorsName = GimbalBall1.009
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.009
            rotatorsName = Gimbal1.009
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.009
            rotatorsName = Gimbal2.009
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.009
            rotatorsName = GimbalBall2.009
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.009
            rotatorsName = TurbopumpBall2.009
			matchRotation = true
        }
	}
     MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.010
            rotatorsName = GimbalBall1.010
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.010
            rotatorsName = Gimbal1.010
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.010
            rotatorsName = Gimbal2.010
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.010
            rotatorsName = GimbalBall2.010
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.010
            rotatorsName = TurbopumpBall2.010
			matchRotation = true
        }
	}
     MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.011
            rotatorsName = GimbalBall1.011
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.011
            rotatorsName = Gimbal1.011
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.011
            rotatorsName = Gimbal2.011
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.011
            rotatorsName = GimbalBall2.011
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.011
            rotatorsName = TurbopumpBall2.011
			matchRotation = true
        }
	}
     MODULE
    {
        name = FXModuleLookAtConstraint

		CONSTRAINLOOKFX
        {
            targetName = Gimbal1.012
            rotatorsName = GimbalBall1.012
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall1.012
            rotatorsName = Gimbal1.012
        }

		CONSTRAINLOOKFX
        {
            targetName = GimbalBall2.012
            rotatorsName = Gimbal2.012
        }

		CONSTRAINLOOKFX
        {
            targetName = Gimbal2.012
            rotatorsName = GimbalBall2.012
        }
	}

	MODULE
    {
        name = FXModuleConstrainPosition

		CONSTRAINFX
		{
            targetName = TurbopumpBall.012
            rotatorsName = TurbopumpBall2.012
			matchRotation = true
        }
	}

    // It's Over... For Now...

}